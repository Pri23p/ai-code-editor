Angular starter — run notes

- Minimal starter files added so the playground can scan the template directory.
- To run locally use the Angular CLI (optional):
  - Install dependencies: `npm install`
  - Install CLI: `npm install -g @angular/cli`
  - Start dev server: `npm run start`

This folder contains a minimal `src` tree (`index.html`, `main.ts`, `app`) so tools that generate a template JSON structure can read the starter files.
